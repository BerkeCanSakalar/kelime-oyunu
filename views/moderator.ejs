<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moderatör Paneli</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            background-color: #1e1e1e;
            color: #f5f5f5;
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            text-align: center;
            padding: 20px;
            background: #292929;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #ffc107;
        }

        p {
            font-size: 1.2rem;
            margin: 10px 0;
        }

        .info-box {
            background: #333;
            padding: 15px;
            border-radius: 8px;
            color: #ffc107;
            font-size: 1rem;
            margin: 20px 0;
        }

        .current-info {
            background: #444;
            padding: 20px;
            border-radius: 10px;
            margin: 15px 0;
            font-size: 1.2rem;
        }

        .current-info span {
            font-weight: bold;
            color: #4caf50;
        }

        .footer {
            margin-top: 30px;
            font-size: 0.9rem;
            color: #777;
        }

        .footer a {
            color: #4caf50;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Moderatör Paneli</h1>
        <div class="info-box">
            Bu ekran yalnızca moderatör tarafından kullanılmalıdır. Sorular ve cevaplar diğer oyuncular tarafından görülmemelidir.
        </div>
        <div class="current-info">
            <p id="current-word">Kelime: <span>Yükleniyor...</span></p>
            <p id="current-meaning">Anlam: <span>Yükleniyor...</span></p>
        </div>
        <div class="footer">
            <p>Bu panel yalnızca moderatör yetkisi olan kullanıcılar için tasarlanmıştır.</p>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const updateWords = () => {
                const savedState = JSON.parse(localStorage.getItem("gameState"));
        
                if (savedState && savedState.currentQuestion) {
                    document.getElementById("current-word").innerHTML = `Kelime: <span>${savedState.currentQuestion.answer}</span>`;
                    document.getElementById("current-meaning").innerHTML = `Anlam: <span>${savedState.currentQuestion.question}</span>`;
                } else {
                    document.getElementById("current-word").innerHTML = "Kelime: <span>Bulunamadı.</span>";
                    document.getElementById("current-meaning").innerHTML = "Anlam: <span>Bulunamadı.</span>";
                }
            };
            updateWords();
            setInterval(updateWords, 1200);
        });
    </script>
</body>
</html>
